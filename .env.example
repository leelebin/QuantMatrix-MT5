NODE_ENV=development
PORT=5000

# JWT (auto-generated if not set)
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRE=24h
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this
JWT_REFRESH_EXPIRE=7d

# Email (optional, for password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=lebin11093@gmail.com
SMTP_PASS=leelebin1093
FROM_EMAIL=noreply@quantmatrix.com
FROM_NAME=QuantMatrix

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5000

# MT5 Broker Connection (direct connection via login/password)
MT5_LOGIN=230044684
MT5_PASSWORD=tE@G6huM
MT5_SERVER=Elev8-Demo2

# Optional: Path to MT5 terminal executable (auto-detected if not set)
# MT5_PATH=C:\Program Files\MetaTrader 5\terminal64.exe

# Optional: Python executable path (defaults to 'python')
# PYTHON_PATH=python

# Telegram Bot Notifications
# 1. Create a bot via @BotFather on Telegram, get the token
# 2. Send a message to your bot, then visit:
#    https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
#    to find your chat_id
TELEGRAM_TOKEN=8081442172:AAG20CBdqzVRdGuLTivqTmUZl7Qp-oT1q0E
TELEGRAM_CHAT_ID=1752223204

# Trading Configuration
MAX_RISK_PER_TRADE=0.05
MAX_DAILY_LOSS=0.25
MAX_DRAWDOWN=0.5
MAX_CONCURRENT_POSITIONS=5
MAX_POSITIONS_PER_SYMBOL=2
TRADING_ENABLED=false
ALLOW_LIVE_TRADING=false

# Paper Trading Mode
PAPER_TRADING_ENABLED=false
# Daily report will be sent to Telegram at this time (24h format, server timezone)
DAILY_REPORT_HOUR=23
DAILY_REPORT_MINUTE=55
